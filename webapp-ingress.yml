apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: webapp-ingress
spec:
  rules:
    - host: url.com  # or you can put a ip of your vp
      paths: /
        pathType: Prefix	
        backend:
          service:
            name: webapp-service
            port:
              number: 80
